# Archive

History only. Nothing here describes the current game; read `docs/HANDOFF.md` and `docs/DECISIONS.md` instead.

| Folder | What |
|---|---|
| `web-build/` | The old web version (branch `web-archive`): its progress log (`PROGRESS.md`), implementation plan (`plan.md`), background art brief, and the UI reference shots and glass experiments. |
| `done/` | Work orders and briefs that are finished: the 2026-09-24 code review fixes (`REVIEW_FIXES.md`), the 2026-09-25 review (`codereview-2026-09-25.md`, fixed; see DECISIONS), the bridge bugs from the first benchmark run (`bridge_bugs.md`, fixed, re-run PASS), the `bugtest_benchmark` build brief (`prompts/`), and the special-recipe naming chunks (`naming/`, final names now live in `data/species.json`). |
